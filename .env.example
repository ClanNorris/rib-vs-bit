# WebSocket server URL for production builds (set as a Netlify build-time env var).
# Local dev does NOT need this — Vite's dev server proxies /ws to localhost:3001 automatically.
VITE_WS_URL=wss://your-railway-app.up.railway.app
